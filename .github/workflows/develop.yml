name: 🚀 Deploy website on push
on:
  push:
    branches:
      - develop

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      server-dir: /dev.johndeguise.com/
    secrets: inherit
